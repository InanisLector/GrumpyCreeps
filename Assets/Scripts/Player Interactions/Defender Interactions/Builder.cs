using UnityEngine;
using GameFlow.GameManager;
using HexGridSystem;

namespace PlayerInteractions.DefenderInteractions.Builder
{
    public class Builder : MonoBehaviour
    {
        //private void TryBuy(BuildingBase building, TileScript centerTile)
        //{
        //    if (building.Cost < GameManager.Instance.DefenderMoney)
        //    {
        //        // Add feedback
        //        return;
        //    }

        //    TryPlace(building, centerTile);
        //}

        //private void TryPlace(BuildingBase building, TileScript centerTile)
        //{
        //    centerTile.CheckFreeTilesInRadius(building.Size);
        //}
    }
}